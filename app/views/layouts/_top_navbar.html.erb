<nav class="bg-cyan-200 border-b border-gray-200  fixed w-full top-0">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
            <div class="flex-shrink-0 text-xl font-bold text-gray-800">
                <%= link_to "Instagram", root_path, class: "hover:text-black" %>
            </div>
            <div class="hidden md:flex space-x-6 items-center">
                <%= link_to "Home", root_path, class: "text-gray-600 hover:text-black" %>
                <% if current_user %>
                    <%= link_to "Profile",  user_path(current_user), class: "text-gray-600 hover:text-black" %>
                    <%= link_to "Find friends", users_path , class: "text-gray-600 hover:text-black" %>
                    <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete}, class: "text-red-500 font-medium hover:text-red-700" %>
                <% else %>
                    <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-600 hover:text-black" %>
                    <%= link_to "Login", new_user_session_path, class: "text-gray-600 hover:text-black" %>
                <% end %>
            </div>

        </div>
    </div>
</nav>